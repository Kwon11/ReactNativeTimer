import React from 'react';
import { View, Text } from 'react-native';

export default class EditPreset extends React.component {
  render () {
    return (
        <Text> EditPreset </Text>
      );
  }
}