import React from 'react';
import { View, Text } from 'react-native';

export default class TimersView extends React.component {
  render () {
    return (
        <Text> TimersView </Text>
      );
  }
}