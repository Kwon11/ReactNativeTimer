import React from 'react';
import { View, Text } from 'react-native';

export default class StaticBottomBar extends React.component {
  render () {
    return (
        <Text> StaticBottomBar </Text>
      );
  }
}