import React from 'react';
import { View, Text } from 'react-native';

export default class CustomTimerAdd extends React.component {
  render () {
    return (
        <Text> CustomTimerAdd </Text>
      );
  }
}